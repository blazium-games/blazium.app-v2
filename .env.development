# TIP: Add a `.env.development.local` file with your machine local IP instead of `localhost`
# for testing in the LAN with other devices.
VITE_API="http://localhost:5173/api"
ORIGIN="http://localhost:5173"